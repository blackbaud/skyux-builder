/*jshint jasmine: true, node: true */
'use strict';

describe('cli utils prepare-library-package', () => {
  const util = require('../cli/utils/prepare-library-package');

  it('should', () => {
    expect(util).toBeDefined();
  });
});
