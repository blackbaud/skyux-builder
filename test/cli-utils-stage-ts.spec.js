/*jshint jasmine: true, node: true */
'use strict';

describe('cli utils prepare-package', () => {
  const util = require('../cli/utils/stage-ts');

  it('should', () => {
    expect(util).toBeDefined();
  });
});
